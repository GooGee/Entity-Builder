export default class Constant {
    static readonly Name = "Laravel Builder"
    static readonly Name2 = "laravel-builder"
    static readonly Folder = Constant.Name2

    static readonly Id = "Id"

    static readonly PackageName = "googee/laravel-builder"

    static readonly ScriptCode =
        "function run(data) {\n    /** @type {LB.DataForScript} */\n    const ddd = data\n\n    /**\n     * write code below\n     * it will be executed before generating files\n     */\n}\n"

    static readonly Zz = "zz"
}
